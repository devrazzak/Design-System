/**
 * Button public API
 * Only export what consumers need - never internal implementation details.
 */
export { Button } from "./button";
export type { ButtonProps, ButtonSize, ButtonVariant } from "./button.types";

console.log("Button component loaded");
